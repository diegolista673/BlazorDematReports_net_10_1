dbcc checkident ('ProcedureCliente',reseed,217)

--insert record a partire dalla riga desiderata di un csv e mantiene identity presente nel csv
BULK INSERT clienti
from 'C:\Users\SMARTW\Desktop\clienti.csv'
with (firstrow = 170,
	  KEEPIDENTITY,
      fieldterminator = ',',
      rowterminator='\n',
      batchsize=10000,
      maxerrors=10);

      nelle query dati migliorare le query eliminando nel where la convert date 
      per data scan o index e indicare la date comprese tra datascan >= @date e dataSacn < @date
      esempio :

      
gestione della configurazione 
finire sql nuova configurazione tramite step da controllare
cosa fare con i task attivarli subito o dopo la creazione della configurazione? 
come eliminarli ?
poterli fermare o eliminarli in caso di errori o modifiche alla configurazione?
la modifica deve permettere di eliminare i task e aggiornare la configurazinione
in modifica deve potere eliminare tutta la configurazione + task associati , domandare prima di eliminare il button in elenco esiste già
eliminare o tenere il button disabilita? e i task associati?
button con task già creati per la configurazione da eliminare se task vengono creati subito in hangfire
LoadConfigurazioniAsync va cambiato per trovare quanti sono i task totali per lavorazione, magari cambiare titolo in task da eseguire piuttosto che configurazioniCount




✅ RISOLTO: Eliminazione task ora funziona correttamente
    - I job vengono rimossi da Hangfire tramite ProductionScheduler.RemoveByKeyAsync()
    - I task vengono eliminati dal database
    - La configurazione e i mapping vengono rimossi
    - Implementato in ServiceConfigurazioneFontiDati.DeleteConfigurazioneFontiDatiAsync()
    
✅ RISOLTO v2: Update configurazione elimina correttamente task orfani (CORREZIONE CRITICA)
    
PROBLEMA ORIGINALE:
- La query LINQ con subquery context.LavorazioniFasiDataReadings.Any() non veniva eseguita correttamente
- I task rimanevano nel DB anche dopo la rimozione dei mapping
- I job Hangfire continuavano ad essere eseguiti
    
SOLUZIONE IMPLEMENTATA (4 STEP):
    
STEP 1: Trova LavorazioniFasiDataReading per il mapping da rimuovere
	var lavorazioneFase = await context.LavorazioniFasiDataReadings
		.FirstOrDefaultAsync(lf => 
			lf.IdProceduraLavorazione == mappingToRemove.IdProceduraLavorazione &&
			lf.IdFaseLavorazione == mappingToRemove.IdFaseLavorazione);
    
STEP 2: Trova i task usando l'ID della lavorazione fase E l'ID configurazione
	var tasksToRemove = await context.TaskDaEseguires
		.Where(t => t.IdConfigurazioneDatabase == config.IdConfigurazione &&
				t.IdLavorazioneFaseDateReading == lavorazioneFase.IdlavorazioneFaseDateReading)
		.ToListAsync();
    
STEP 3: Rimuovi job da Hangfire (PRIMA del DB)
	await productionScheduler.RemoveByKeyAsync(task.IdTaskHangFire);
    
STEP 4: Elimina task dal database
	context.TaskDaEseguires.RemoveRange(tasksToRemove);
    
CLEANUP FINALE (post-commit):
	await productionScheduler.CleanupOrphansAsync();
	- Confronta job Hangfire con task nel DB
	- Rimuove job orfani rimasti (doppia sicurezza)
    
LOGGING:
- ✅ = Job Hangfire rimosso con successo
- 🗑️ = Task eliminati dal database
- 🧹 = Cleanup finale eseguito
- ❌ = Errore (logged ma non blocca l'operazione)
    
RISULTATO:
✅ Task vengono eliminati correttamente dal DB
✅ Job Hangfire vengono rimossi correttamente
✅ Nessun job "fantasma" continua ad eseguirsi
✅ Logging completo per debugging



come eseguire test su email se non leggo edpvr? intanto leggere csv di esempio da cartella di test e poi provare su macchina uffico 

implementare la configurazione per il servizio mail
implementare la logica per gli handler
implemetnare la logica per i multi step hanlder?



semplificare o capiere bene le configurazioni json parametri per colonne ecc.

va rivisto il discorso delle quey che deveono diventare personalizzate per utente come in mondo
come conteggiare i servizi mail associati alla lavorazione?
controllare le lavorazioni che sono in datareading true ma non hanno un job associato



TODO
Per le tabelle di appoggio stabilire come eseguire query di controllo per vedere se tutti i dati sono stati caricati esempio ader mensile totali per fatturazione coop
visualizzare le tabelle di appoggio?

Controllare le varie operazioni fatte con utenti e permessi diversi e ruoli soprattutto per elenco lavoratori, schede totali e impostazioni

Altra utenza quante volte possibile ? pensare di attribuire ad uno scanner piu di un operatore.

Condizione obbligatoria è che ci sia il risultato in Operatore,datalavorazione,Documenti,fogli pagine puoè valere per tutte le lavorazioni?
testare query dedicata con qualche query integrta che ha campi data diversi 



Per quando programma è pronto per la produzione
    database in produzione salvato, importare eventuali procedure nuove , importare produzione sistema  con identity on e produzione operatori
    modificare documenti isoledigitali ge 2024 2025
    aggiungere colonna [UtilizzataDaSistema] in tabella fasi lavorazione per distinguere fasi create dal sistema e non modificabili
    (tutte a false di default tranne procedura completa e no_fase_lavorazione)


 




controllare il funzinamento in lettore dati, job mail e job lavorazioni

scrivere documetazione per aggiungere servizi via mail per programmatore eventaulmente aggiungere grafico delle attivita /sequenze

creareservizio mail per ader 4 verona e genova







